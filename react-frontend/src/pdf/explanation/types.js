// Explanation types (JS - no TypeScript interfaces needed, just for reference)
// ExplanationData: { term, meaning, page }
// ExplanationPosition: { startOffset, endOffset, pageNumber }
// StoredExplanation: { id, selectedText, explanation, position, createdAt, pageNumber }

// These are just used as plain objects in JavaScript
export default {};
